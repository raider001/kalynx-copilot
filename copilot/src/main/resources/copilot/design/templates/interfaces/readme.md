# Interfaces

<!--
AI: Each interface gets its own file in this directory, named after the interface.
Use _template.md as the starting point when creating a new interface document.
Prefer narrow, focused interfaces. If an interface has more than ~7 methods,
question whether it should be split. All interfaces defined here are authoritative —
implementations must conform to these contracts.
-->

## Index

| Interface | Purpose | File |
|-----------|---------|------|
| *(none yet)* | | |

## Design Rules

<!-- Rules that apply to all interfaces in this project. -->

- Interfaces define contracts, not implementation details
- Every method must have documented parameters, return type, and failure conditions
- Prefer checked exceptions for recoverable errors, runtime for programming errors
